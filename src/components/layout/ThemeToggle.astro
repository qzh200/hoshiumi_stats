---
/**
 * ThemeToggle —— 右上角三态切换（浅色 / 深色 / 跟随系统）
 *
 * 图标采用与 hoshiumi_home 完全一致的 Lucide 路径（sun / moon / monitor），
 * 由 :root[data-theme-choice] 驱动显示哪一个，保证行为和视觉与 home 一致。
 */
const ICONS = [
  { choice: 'light', icon: 'sun' },
  { choice: 'dark', icon: 'moon' },
  { choice: 'system', icon: 'monitor' },
] as const;
---

<button class="theme-toggle" type="button" data-theme-toggle aria-label="切换主题（浅色 / 深色 / 跟随系统）">
  <span class="theme-toggle__icon theme-toggle__icon--light" aria-hidden="true">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
      <circle cx="12" cy="12" r="4" />
      <path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.41-1.41M17.66 6.34l1.41-1.41" />
    </svg>
  </span>
  <span class="theme-toggle__icon theme-toggle__icon--dark" aria-hidden="true">
    <svg viewBox="0 0 24 24" fill="currentColor">
      <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z" />
    </svg>
  </span>
  <span class="theme-toggle__icon theme-toggle__icon--system" aria-hidden="true">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
      <rect x="3" y="4" width="18" height="14" rx="2" />
      <path d="M8 20h8M12 18v2" />
    </svg>
  </span>
</button>

<script>
  // 三态轮转：light → dark → system → light …
  const order = ['light', 'dark', 'system'] as const;
  const button = document.querySelector<HTMLButtonElement>('[data-theme-toggle]');
  const win = window as unknown as {
    statsTheme?: { get: () => string; set: (c: string) => void };
  };

  if (button && win.statsTheme) {
    button.addEventListener('click', () => {
      const current = win.statsTheme!.get() as (typeof order)[number];
      const idx = order.indexOf(current);
      win.statsTheme!.set(order[(idx + 1) % order.length]);
    });
  }
</script>
